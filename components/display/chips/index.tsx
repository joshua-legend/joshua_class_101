import React from "react";
import {Container} from "@/components/display/chips/styles";

interface IChip {

}

const Chip = ({}: IChip) => {
    return (
        <Container>
            chip
        </Container>
    );
}

export default Chip;